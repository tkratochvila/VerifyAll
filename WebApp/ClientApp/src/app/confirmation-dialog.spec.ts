import { ConfirmationDialog } from './confirmation-dialog';

describe('ConfirmationDialog', () => {
  it('should create an instance', () => {
    expect(new ConfirmationDialog()).toBeTruthy();
  });
});
