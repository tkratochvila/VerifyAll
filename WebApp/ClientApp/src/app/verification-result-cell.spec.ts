import { VerificationResultCell } from './verification-result-cell';

describe('VerificationResultCell', () => {
  it('should create an instance', () => {
    expect(new VerificationResultCell()).toBeTruthy();
  });
});
