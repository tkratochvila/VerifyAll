import { VerificationResults } from './verification-results';

describe('VerificationResults', () => {
  it('should create an instance', () => {
    expect(new VerificationResults()).toBeTruthy();
  });
});
